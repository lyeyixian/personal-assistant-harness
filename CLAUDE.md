# personal-assistant-harness

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`lyeyixian/personal-assistant-harness`), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Two roles in active use (`ready-for-agent`, `wontfix`) out of the five-role default vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
