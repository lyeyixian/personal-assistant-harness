"""Journal capture — the Experience Store's write path, per ADR-0002.

`add_entry`/`list_entries` are deterministic; no agent lives here yet
(the Fold agent that curates pending entries into notes is a later ticket).
"""
